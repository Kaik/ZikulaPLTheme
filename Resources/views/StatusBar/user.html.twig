<!-- User Account: style can be found in dropdown.less -->
<li class="dropdown user user-menu">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        <img src="{{ zasset('@ZikulaPLTheme:images/default-user.png') }}" class="user-image" alt="User Image">
        <span class="hidden-xs">Alexander Pierce</span>
    </a>
    <ul class="dropdown-menu">
        <!-- User image -->
        <li class="user-header">
            <img src="{{ zasset('@ZikulaPLTheme:images/default-user.png') }}" class="img-circle" alt="User Image">

            <p>
                Alexander Pierce - Web Developer
                <small>Member since Nov. 2012</small>
            </p>
        </li>
        <!-- Menu Body -->
        <li class="user-body">
            <div class="row">
                <div class="col-xs-4 text-center">
                    <a href="#">{{__('Messages')}}</a>
                </div>
                <div class="col-xs-4 text-center">
                    <a href="#">{{__('Notifications')}}</a>
                </div>
                <div class="col-xs-4 text-center">
                    <a href="#">{{__('Tasks')}}</a>
                </div>
            </div>
            <!-- /.row -->
        </li>
        <!-- Menu Footer-->
        <li class="user-footer">
            <div class="pull-left">
                <a href="{#{{ path('zikulausersmodule_user_index') }}#}" class="btn btn-default btn-flat">{{ __('Profile') }}</a>
            </div>
            <div class="pull-right">
                <a href="{#{{ path('zikulausersmodule_user_logout') }}#}" class="btn btn-default btn-flat">{{ __('Sign out') }}</a>
            </div>
        </li>
    </ul>
</li>